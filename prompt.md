Analyze the sales dataset in `data/sales_data.csv` and build an interactive executive dashboard as a single HTML file.

1. Inspect the dataset and summarize what you find (columns, data quality issues)
2. Clean the data and handle any missing values
3. Build `dashboard.html` in this folder with:
   - KPI cards: total revenue, YoY growth, average profit margin, total orders
   - Monthly revenue trend (2024 vs 2025)
   - Revenue by region (bar chart)
   - Revenue by product category
   - Top 10 sales reps by revenue

Use Python for analysis. The dashboard must be a single self-contained HTML file (inline CSS/JS, no external dependencies) that opens in any browser. Make it professional and executive-ready.
